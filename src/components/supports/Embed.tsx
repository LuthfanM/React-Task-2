import React from 'react'

interface EmbedProps {
    embed?: string,
    width? : string,
    heigth? : string,
}

const Embed: React.FC<EmbedProps> = ({
    embed,
    width,
    heigth
}) => {
  return (
    
  )
}

export default Embed