---
import ReactSpacer from "./ReactSpacer.tsx";
import SpacerData from "@/bookshop/data/Spacer.bookshop.json";

const props = {
  maxHeight: SpacerData.maxHeight,
  maxWidth: SpacerData.maxWidth,
};
---

<ReactSpacer {...props} />
