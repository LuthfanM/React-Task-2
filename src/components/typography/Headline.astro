---
import ReactHeadline from "./ReactHeadline";
import HeadlineData from "@/bookshop/data/Headline.bookshop.json";

const props = {
    title: HeadlineData.title,
      kicker: HeadlineData.kicker,
      subhead: HeadlineData.subhead,
      style: HeadlineData.style,
}
---

<ReactHeadline {...props} />